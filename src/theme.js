import { createTheme } from "@mui/material";

const theme = createTheme({
    
    palette: {
        mode: "light",
        primary: {main: "#6D213C"},
        secondary:{ main: "#946846"},
        error: {main: "#BAAB68"}
    }
})
export default theme